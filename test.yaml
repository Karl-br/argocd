argo_namespace: argocd

destination_server: 

targetRevision: main

repoURL: https://github.com/Karl-br/ts0624.git

project: default

apps:
    - name: api-auth
      namespace: api-auth
      helmfile: api-auth.yaml
